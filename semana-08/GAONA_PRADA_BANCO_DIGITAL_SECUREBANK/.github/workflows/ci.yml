name: SecureBank CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r semana_8/requirements-dev.txt

      - name: Format check with Black
        run: |
          black --check semana_8/app/ semana_8/tests/

      - name: Import sorting check with isort
        run: |
          isort --check-only semana_8/app/ semana_8/tests/

      - name: Lint with flake8
        run: |
          flake8 semana_8/app/ semana_8/tests/

      - name: Type checking with mypy
        run: |
          mypy semana_8/app/

      - name: Security check with bandit
        run: |
          bandit -r semana_8/app/

      - name: Run tests
        run: |
          pytest semana_8/tests/ -v --cov=semana_8/app --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: unittests
          name: securebank-codecov